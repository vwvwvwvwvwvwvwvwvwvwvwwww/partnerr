# Тот же хост, что в адресной строке (127.0.0.1 ≠ localhost для cookies)
VITE_API_URL=http://127.0.0.1:4010/api
VITE_CSRF_COOKIE_NAME=csrf_token

# Порт сайта (Vite). При занятом порте Vite возьмёт следующий — смотрите строку Local в терминале.
VITE_DEV_PORT=8848
