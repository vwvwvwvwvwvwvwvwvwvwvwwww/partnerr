# Скопируйте в .env.production перед сборкой для хостинга.
# Один домен: API и сайт с одного origin — относительный путь (рекомендуется при раздаче SPA из backend).
VITE_API_URL=/api

# Если фронт на CDN, а API на другом поддомене — полный URL, например:
# VITE_API_URL=https://api.example.com/api
