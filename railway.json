{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm install && npm run install:all && npm rebuild better-sqlite3 --prefix backend && npm run build && npm run migrate --prefix backend"
  },
  "deploy": {
    "startCommand": "node scripts/railway-start.mjs",
    "restartPolicyType": "ON_FAILURE"
  }
}
