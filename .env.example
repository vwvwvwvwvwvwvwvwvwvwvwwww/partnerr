# Корневой пример (основные настройки — в backend/.env и frontend/.env)
# Скопируйте при необходимости или выполните: npm run bootstrap

NODE_ENV=development

# Backend — см. backend/.env.example (DB_DRIVER=sqlite по умолчанию)
# Frontend — см. frontend/.env.example (VITE_API_URL, VITE_DEV_PORT)
